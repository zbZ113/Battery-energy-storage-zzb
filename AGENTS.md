# Codex 协作规则

本文件适用于整个仓库。进入子目录时，如存在更具体的 `AGENTS.md`，以更具体规则为补充。

## 不可违反的项目规则

1. LLM 只能编排工具与解释工具结果，不能生成 SOH、RUL、置信区间、阈值比较或经营指标等数值。
2. 每个业务数值必须可追溯到有效 `ToolResult`；不得在 Agent 提示词或 UI 中硬编码“示例结果”冒充计算结果。
3. 未经来源核验，禁止加载 pickle、joblib、`.pt`、`.pth` 或其他可执行反序列化制品。优先采用 safetensors、ONNX、JSON、Parquet；模型制品必须校验 SHA-256 与清单。
4. 数据集必须按 `cell_id` 分割；任何按行、按周期随机分割导致同一电芯跨集合的实现都应被拒绝。
5. PyBaMM 仅用于短时滚动物理核验与敏感性参考，不得制造长期退化标签或替代真实测试数据。
6. 所有接口复用 `quanxin_life.core` 公共枚举和 Pydantic 契约，不另造语义相同但字段不同的 DTO。
7. 不确定、缺测、域外输入必须显式降级；不得静默填补或伪造来源。

## 工程方式

- 先阅读批准计划、项目宪法、数据契约和实验协议，再修改代码。
- 新功能和缺陷修复严格执行测试先行；先观察目标测试因预期原因失败，再写最小实现。
- 使用小而可审查的提交，避免混入无关重构；保留用户已有改动。
- Python 版本必须为 3.11，包采用 `src/quanxin_life` 布局；高级能力放入可选依赖，核心导入不得触发模型、网络或服务初始化。
- 外部接口、模型、数据、特征和提示词都要版本化。时间必须带时区并归一到 UTC。
- 日志不得包含密钥、原始敏感数据或完整用户提示；所有外部输入都按不可信处理。

## 多代理协作

- 根 Agent 负责范围、依赖顺序、集成与最终验证；子 Agent 只处理边界清晰且互不写冲突的任务。
- 并行任务不得同时编辑同一文件。开始前声明文件所有权，完成后回报变更、测试和风险。
- 子 Agent 不得自行扩大范围、提交、推送或修改公共契约；公共契约变更必须由根 Agent 审核。
- Agent 报告不能替代验证。集成人必须检查差异并重新运行项目门禁。

## 最低验证

按变更范围运行 pytest、ruff、mypy 和构建/编译检查。没有最新命令输出，不得声称完成或通过。
