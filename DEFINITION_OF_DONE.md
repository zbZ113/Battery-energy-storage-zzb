# 完成定义

一个变更只有同时满足以下条件才可标记完成：

## 需求与契约

- 验收标准可追溯到批准计划，范围外内容已明确记录。
- 公共枚举、Pydantic 模式、数据标签、单位、版本和失败语义没有出现重复定义。
- 所有业务数值均来自有效 `ToolResult`；来源、输入哈希、不确定性、警告和 UTC 时间完整。

## 数据、模型与安全

- 数据按 `cell_id` 隔离，拟合过程没有测试集泄漏；右删失与 EOL 定义符合数据契约。
- 输入、数据、特征、模型和工具版本可复现；模型制品经过清单与 SHA-256 校验。
- 域外、缺测和冲突证据有显式降级；PyBaMM 模拟未冒充观测真值。
- 外部输入、密钥、许可证、隐私、BMS/EMS 控制边界经过审查。

## 工程质量

- 新行为先有失败测试，随后以最小实现转绿；相关单元、集成与契约测试通过。
- Ruff、mypy、编译/构建及适用的前端和容器检查通过，且输出来自当前提交。
- 文档、配置示例、迁移/回滚说明和第三方声明已同步；没有提交生成物、密钥或大文件。
- 变更由非作者或监督器完成差异自审，阻断级问题为零，剩余风险和证据限制已在交付报告中说明。
