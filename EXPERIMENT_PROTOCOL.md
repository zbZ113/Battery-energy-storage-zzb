# 实验协议

## 冻结顺序

先冻结问题、数据快照、适配器版本、SOH/EOL/删失定义和电芯级划分，再拟合特征与模型。实验记录必须包含代码提交、Python/依赖环境、随机种子、硬件、输入哈希、特征版本、模型版本和完整配置。

## 数据与任务

基准数据覆盖 MATR、HUST 与 Naumann，分别形成可复现数据卡。评估包含同域电芯泛化、跨协议泛化、跨数据集域适应和滚动截断预测。任何删除电芯或周期的规则必须在查看测试结果前确定，并报告数量与原因。

## 评价

SOH 与 RUL 使用经工具计算的预注册误差、排序、校准和区间覆盖指标；RUL 需区分已发生 EOL 与右删失样本。所有聚合先在电芯内计算，再跨电芯汇总，并提供电芯级重采样不确定性。模型选择只看验证集，同时与朴素、统计、树模型和序列模型基线比较。

诊断评价需说明异常定义、提前量、误报/漏报代价和协议变化混淆。决策评价需同时报告收益与安全护栏，不允许只优化单一平均指标。

## 安全与复现

模型制品从受控训练流程生成，保存清单和哈希；不加载未知 pickle/joblib/pth。PyBaMM 只对短期窗口做趋势、边界与敏感性核验，其模拟结果不得混入观测标签。每次正式报告必须能从冻结清单重跑，失败实验和负结果同样保留。
