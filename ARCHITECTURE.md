# 架构说明

## 逻辑分层

1. **接入层**：MATR、HUST、Naumann 等公开数据适配器，以及经授权的 BMS/EMS 流式和批量接口。
2. **数据与特征层**：不可变原始区、标准化电芯周期表、质量规则、版本化特征和电芯级划分清单。
3. **工具层**：SOH、RUL、异常/退化诊断、域检测、不确定性、短期 PyBaMM 核验和报告渲染工具，统一返回 `ToolResult`。
4. **智能体层**：数据 Agent、预测 Agent、诊断 Agent、决策 Agent，由监督器执行状态机、权限、证据校验与失败降级。
5. **应用层**：FastAPI 服务、Next.js 业务门户、Streamlit 研究分析台，以及飞书通知与知识库检索入口。
6. **基础设施层**：PostgreSQL 元数据、对象存储、Redis 任务/缓存、可观测性与 WSL2 + Docker 部署。

## 运行原则

监督器将请求写入 `AnalysisState`，各 Agent 只通过注册工具读取输入和产出结果 ID。工具结果先进行 Pydantic 校验、来源校验和数值有限性校验，再进入证据图。最终建议由规则与工具计算决策枚举，LLM 仅把结构化证据转述为自然语言。

核心包不得依赖数据库、Web 框架运行时、模型权重或 Agent 框架。服务通过依赖注入组合高级模块，避免导入即连接网络或加载权重。

## 信任边界与失败策略

数据上传、MCP、BMS/EMS、飞书、知识库和 LLM 都是外部边界。所有载荷做模式、大小、权限、哈希与超时校验；密钥只从运行环境注入。工具失败保留已验证结果并发出警告，证据不足时停止升级结论。任何控制建议都不能绕过既有 BMS/EMS 安全联锁。
