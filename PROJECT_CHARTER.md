# 项目宪法

## 使命与边界

泉芯智寿面向储能电芯研发、生产、运维与梯次利用场景，以可审计数据、统计/机器学习模型、短期物理核验和多智能体协作为基础，提供 SOH 估计、剩余寿命预测、退化诊断、风险分层与决策报告。

系统提供决策支持而非安全认证、质保裁决或无人值守控制。任何进入 BMS/EMS 的动作默认是建议，必须经过授权、策略校验与人工或既有安全控制链确认。

## 宪法级约束

- 数值权威属于工具，不属于 LLM。Agent 只能引用已校验 `ToolResult` 中的值。
- 结论必须区分直接数据、模型推断、物理参考、领域知识与无法判定五类证据。
- 数据、特征、模型、工具、提示词和报告模板必须版本化；输入与制品必须带 SHA-256。
- 实验以电芯为最小隔离单元；测试集只用于最终评价，任何调参回看都使其失效。
- 不安全反序列化、无来源数据、跨集合泄漏、无不确定性模型输出均不得进入生产证据链。
- 域外、低质量或相互冲突的证据应触发 `RECHECK`、`DOWNGRADE` 或 `REJECT`，而非强行给出乐观结论。
- PyBaMM 输出标记为 `SIMULATED`/`PHYSICS_REFERENCE`，只承担短期一致性检查。

## 治理

公共契约、标签定义、数据划分和放行阈值的改变视为破坏性变更，必须有设计记录、迁移方案、回归测试和负责人批准。研发、数据、模型、产品和安全负责人分别对实现正确性、数据授权、模型风险、业务表达和系统边界负责。
