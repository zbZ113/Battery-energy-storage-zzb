# HUST 公开电池数据卡 v1

## 1. 证据状态

| 字段 | 当前记录 |
|---|---|
| 数据集标识 | `HUST` |
| 上游版本 | `Mendeley-v2` |
| 来源 | `https://data.mendeley.com/datasets/nsc7hnsg4s/2` |
| 论文 DOI | `10.1039/D2EE01676A` |
| 许可证记录 | `CC BY 4.0`，来自下载时记录，公开发布前仍需联网复核上游页面 |
| 本地取回时间 | `2026-07-13T11:16:07.228047Z` |
| 压缩包逻辑名 | `our_data.zip` |
| 压缩包大小 | `1,188,136,932` bytes |
| 压缩包 SHA-256 | `071d24617153693b0d29059568525e620f6af6512acc9d00c98c7adcf15125db` |
| 不透明成员数 | 77 |
| 成员压缩后合计 | `1,188,122,260` bytes |
| 成员解压流合计 | `4,245,730,747` bytes |
| 原始成员格式 | Python pickle（不可信可执行反序列化格式） |
| 主应用直接加载 | 禁止 |
| inventory 版本 | `hust-mendeley-v2-local-bytes-v1` |
| 候选 inventory SHA-256 | `2e1d389b8efcb53d78a6656289e5a81712c54524d3581c4d06316bad4970e2c5` |
| 批准 inventory SHA-256 | `2cd660284f1eef30e433e70370a2e8cfa4892072c7f5e29e962a44719a491405` |
| 人工批准 | `Zibin` 于 `2026-07-17T01:40:21.809485Z` 批准 |
| 解压成员复核 | 77 个文件的大小和 SHA-256 均与候选 inventory 匹配 |
| 原始成员位置 | 已移至工作区外 `D:\guet_learning\26 AI acting\hust-quarantine-raw` |
| 安全转换 | 仍阻断：原始 ZIP 当前不可用，尚未完成批准清单约束下的二次审计 |

上述大小、压缩包哈希、成员数量和成员字节流哈希属于本机实际复核事实。来源、论文和许可证属于下载时记录的上游元数据；断网期间不能把它们描述为“已于当前日期在线复核”。

## 2. 当前审计做了什么

- 校验外层 ZIP 的大小与 SHA-256；
- 检查中央目录、压缩方法、成员路径、大小、压缩比和文件类型；
- 对每个 `.pkl` 成员以不透明字节流方式计算 SHA-256；
- 不调用 `pickle.load`，不提取到转换目录，不执行成员中的 Python 对象；
- 生成候选冻结清单，并由项目负责人确认候选摘要后生成批准清单；
- 将 77 个已解压成员作为不透明字节流逐一与候选清单核对，未执行 pickle；
- 将匹配的原始成员移至工作区外隔离目录；
- 原始 ZIP 当前不可用，因此尚未执行批准清单约束下的二次 ZIP 审计，`ready_for_conversion` 仍不得设为 `true`。

## 3. 当前尚未确认的内容

在隔离转换和字段审核完成之前，不能确认：

- 77 个文件是否各自对应 77 个有效电芯；
- pickle 内部字段、单位、循环编号、容量、温度、SOH 或 EOL 标签；
- 每个对象是否安全、完整且满足项目统一数据契约；
- 是否已经形成可信 Parquet、固定电芯划分或模型训练输入；
- HUST 与海辰储能电芯、储能电站 15–25 年运行或工业质保之间存在直接等价关系。

## 4. 对外表述边界

允许表述：

> 项目已对一个登记为 HUST Mendeley v2 的本地 ZIP 快照完成不透明字节静态审计，冻结清单已经人工批准；77 个已解压成员的大小与 SHA-256 均与清单匹配，并已移至工作区外隔离目录。由于原始 ZIP 当前不可用，批准后的二次 ZIP 审计和隔离字段发现尚未完成，原始 pickle 从未进入主应用、正式服务器或训练环境。

禁止表述：

- “HUST 数据已经安全转换完成”；
- “已经验证 77 个电芯的 SOH/RUL”；
- “已经完成 HUST 模型训练或跨域实验”；
- “该数据来自海辰储能”或“可直接代表海辰工业电芯”；
- “许可证已在 2026 年 7 月 16 日在线重新确认”。

## 5. 后续更新条件

只有依次完成以下工作，才能更新转换状态：

1. 重新取得哈希为 `071d24617153693b0d29059568525e620f6af6512acc9d00c98c7adcf15125db` 的原始 ZIP；
2. 使用批准 inventory 对原始 ZIP 再次全量静态审计；
3. 在无网络、无密钥、只读输入、非 root 的一次性隔离环境进行字段发现；
4. 人工审核字段和单位映射；
5. 逐文件转换为 Parquet 与 JSON 元数据；
6. 主项目重新验证安全产物、数据契约、哈希和 `cell_id` 划分。
