# ECS 只读审计：2026-08-02

## 审计边界

本次审计通过阿里云 VNC 本机控制台执行，只读取系统、Docker、数据库、队列、TLS、
备份和审计记录状态。未执行 `pull`、`up`、restart、升级、清理、删除、配置修改或业务
运行创建；未读取、输出或保存密码、私钥、token、连接串或 secret 文件内容。

当前实例：

| 项目 | 事实 |
| --- | --- |
| 公网 IP | `47.99.69.138` |
| 私网 IP | `172.25.232.189` |
| 操作系统 | Ubuntu 22.04.5 LTS，Linux `5.15.0-186-generic` |
| 规格 | 4 vCPU，约 7.1 GiB 可用内存，2 GiB swap，KVM |
| Docker | Engine `29.0.2`，Compose `v5.3.1` |
| 宿主机安全 | UFW active，默认拒绝入站；80/443/22 有允许规则，SSH 来源限制实际依赖 ECS 安全组 |

公网 22 已临时放行审计电脑的 `/32` 地址。端口可达后，现有本地项目密钥对
`root`、`ubuntu`、`ecs-user` 均返回 `Permission denied (publickey)`，因此没有通过 SSH
修改服务器或注入新密钥。审计使用用户亲自输入密码后的 VNC root 会话。

## 系统与容量

- 4 个逻辑 CPU，Intel Xeon Platinum，1 socket、2 cores/socket、2 threads/core。
- 审计时内存约 1.3 GiB 已用，约 5.5 GiB available；swap 未使用。
- 根文件系统约 40 GiB，约 27 GiB 已用、11 GiB 可用，使用率约 72%。
- inode 使用率约 9%，当前不是 inode 枯竭。
- `/var/lib/docker` 约 13 GiB，`/srv/quanxin` 约 557 MiB。
- Docker 镜像共 7 个、约 21.1 GB，其中约 9.921 GB 标为 reclaimable；本次未清理。
- Docker 没有 named volume，运行数据依赖宿主机 bind mount。
- systemd journal 占用约 32 MB。

## 网络、服务与 TLS

- 宿主机监听 22；Docker proxy 监听 80/443；PostgreSQL、Redis、API、Frontend 未直接暴露公网。
- Docker 与 SSH 服务 active；宿主机 Nginx、PostgreSQL、Redis 服务 inactive，符合容器化部署。
- 严格 TLS 请求：`/health=200`、`/=307`、未认证 `/v1/projects=401`。
- 证书由 Let's Encrypt `YE2` 签发，当前证书有效期截止
  `2026-08-08 02:50:38 UTC`。
- `snap.certbot.renew.timer` active；2026-08-02 04:51 和 13:28 CST 的 renew service
  均成功。2026-08-01 曾失败一次，当前已恢复，但短周期证书仍需持续监控。
- Certbot `5.7.0`，OpenSSH `8.9p1 Ubuntu-3ubuntu0.16`，UFW `0.36.1`。

## 当前运行栈

Compose 配置可解析，包含 PostgreSQL、Redis、migrate、API、Worker、Frontend、Gateway。
当前容器事实：

| 服务 | 当前状态 | 当前镜像 tag |
| --- | --- | --- |
| API | running，healthy | `quanxin-backend:2026.07.29-1` |
| Worker | running，Celery `pong` | `quanxin-backend:2026.07.29-1` |
| Frontend | running，healthy | `quanxin-frontend:2026.07.29-1` |
| Gateway | running，healthy | `quanxin-nginx:2026.07.29-1` |
| PostgreSQL | running，healthy | `quanxin-postgres:2026.07.29-1` |
| Redis | running，healthy | `quanxin-redis:2026.07.29-1` |
| migrate | exited `0` | `quanxin-backend:2026.07.29-1` |

因此 ECS 没有部署当前已发布的 `2026.08.02-1`。运行镜像 RepoDigest 与该 release 文档
中的五个 digest 不一致，且运行镜像的
`org.opencontainers.image.revision` / `org.opencontainers.image.version` 标签为空。

当前仍是旧的可变热修复部署：

- API/Worker 从 `/srv/quanxin/hotfixes/calibration-runtime-v7/` 将多个 Python 单文件绑定到
  容器 `site-packages`；
- deployment registry、calibration evidence、data、artifacts、policies 和 config 均为
  宿主机 bind mount；
- Gateway 仍将 `/srv/quanxin/deploy/nginx/competition.conf` 绑定到
  `/etc/nginx/conf.d/default.conf`；
- 这与 `2026.08.02-1` 的镜像内置 Gateway 配置、无运行时代码覆盖目标不一致。

当前状态只能记为：新 release `Published`，ECS 上存在健康的旧 release 部署；不能将
`2026.08.02-1` 标为 `Deployed` 或 `Demonstrated`。

## 数据库、Redis 与 Agent

- migrate 于 `2026-07-30` 以 `exit=0` 完成，日志包含 `DATABASE_MIGRATIONS_OK`。
- PostgreSQL Alembic 版本为 `0015`，数据库约 13 MB。
- Worker 返回 Celery `pong`。
- Redis 容器健康，但近 24 小时日志出现 68 条匹配 warning/error 的记录，主要是 AOF
  loading client 在 `EXEC` 阶段遇到 ACL `NOPERM` 的 CRITICAL 错误。
- 在不读取凭据的边界内，Redis `INFO persistence` 查询未能通过 ACL 认证，因此不能证明
  AOF 恢复完整；需要在受控运维窗口使用既有 secret 机制做专项核验。
- Gateway 的 3 条错误均为启动阶段访问 API 时的短暂 `connection refused`。

数据库只有一个历史 Agent run：

- 状态 `COMPLETED`；
- 9 个固定步骤、9 条 ToolResult，全部完成且无错误码；
- 关联 23 条 provenance 记录；
- `reports` 表关联数为 0；
- run 创建于 2026-07-31，早于当前容器启动和 `2026.08.02-1` 发布。

该记录证明历史九步 ToolResult 闭环，不证明当前 release；报告步骤完成但数据库报告关联
缺失或采用其他存储路径，仍需产品化修复和真实 E2E 验收。

## 备份与证据

- `/srv/quanxin/backups` 约 17 MB，共 36 个 PostgreSQL dump；最新可见备份时间为
  `2026-08-01 20:25`。
- `/srv/quanxin/calibration-evidence` 约 57 MB，共 53 个文件。
- `/srv/quanxin/data` 约 30 MB，共 8 个文件。
- `/srv/quanxin/deployment-registry/records` 有 3 条部署记录。
- `/srv/quanxin/evidence` 包含高级工具链、失败尝试、exact Agent 与 `.sha256` 证据文件。
- 本次只确认文件元数据和现有 SHA 文件存在，没有执行备份恢复或重新计算全部证据清单。

## 风险与后续

1. **发布错位**：ECS 仍运行 `2026.07.29-1` 和热修复覆盖，当前不可变 release 尚未部署。
2. **Redis 恢复风险**：AOF 回放出现 ACL `NOPERM`，持久化完整性尚未证明。
3. **磁盘风险**：根盘约 72%，Docker 镜像占 21.1 GB；清理前必须先确认回滚与备份需求。
4. **报告链缺口**：历史九步 run 有 ToolResult/provenance，但 `reports` 表无关联记录。
5. **证书运维**：snap 自动续期当前成功，但证书周期短且曾有失败记录，需要告警和演练。
6. **管理通道**：SSH 端口可达但现有公钥未绑定；不要开启密码 SSH 或开放 `0.0.0.0/0`。

Task 3 源码开发可以继续。部署 `2026.08.02-1`、Redis 恢复专项、备份恢复、回滚、真实
九步 E2E 与临时安全组规则收口归入 Task 8，并必须在执行前重新备份和记录当前旧部署。
