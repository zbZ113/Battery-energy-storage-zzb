# 数据契约

## 基本实体

每条记录必须能关联唯一 `dataset_id` 与数据集内唯一 `cell_id`。原始文件保留 URI、SHA-256、采集/发布信息、授权说明和模式版本。标准化周期记录至少包含电芯、周期序号、时间、工况、容量/能量观测与质量标记；字段单位必须在模式注册表中固定，禁止凭名称猜单位。

## 标签定义

- 参考容量 `C_ref`：由数据集适配器按已登记协议确定的基准容量；不得在测试集上重新选择使结果更优的参考窗口。
- `SOH_t = C_t / C_ref`，其中 `C_t` 与 `C_ref` 使用同一容量定义和单位。
- `EOL80`：首次有效观测满足 `SOH_t <= 0.80`，且后续两个有效诊断点持续满足 `SOH <= 0.80` 的周期。若后续确认点不足或观察窗内未形成持续越阈，标记为右删失，不能把最后周期当作 EOL。
- 截断点 `k`：预测时只允许使用周期 `<= k` 的观测和当时可获得的元数据；`k` 之后的数据只用于标签与评价。
- 在截断点的周期 RUL 为 `max(EOL80_cycle - k, 0)`；未观察到 EOL80 时必须携带删失标记，不能伪造点标签。

每个 `CellMetadata` 明确实际使用的 `reference_capacity_ah` 与 `eol_threshold`；默认阈值为 0.8。任何偏离须作为独立实验协议版本记录。

## 划分与防泄漏

训练、验证、Conformal 校准、测试按完整 `cell_id` 分组，一枚电芯只能属于一个集合；同数据集默认按 60%/15%/10%/15% 划分，并按寿命分位数和协议分层。标准化器、特征筛选和模型参数只在训练集拟合；验证集用于模型选择；校准集只拟合预测区间；测试集一次性用于最终报告。跨数据集域适应需同时报告源域、目标域无标签/少标签设置、独立目标域重校准电芯及从未参与适配的目标测试电芯。

## 来源与质量

原始、预测、新观测和模拟数据分别使用 `OBSERVED`、`PREDICTED`、`NEWLY_OBSERVED`、`SIMULATED`。缺失、重复周期、非单调时间、单位冲突、容量异常与协议变化必须产生可机读质量标记。未经许可或哈希不匹配的数据不得进入训练和报告。
